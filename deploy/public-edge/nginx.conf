# FE public-edge entry for self-hosted / CN ECS.
# Intl production uses Netlify edge (netlify/edge-functions/rum-ingest.js).
# KAZI-567 rum ingest is included here — not an unused sample.

include rum-ingest.conf;
